%p Hej!
%p Du har påbörjat deltagaranmälan till #{@team.race.regatta.name} som arrangeras av #{@team.race.regatta.organizer.name}.
%p Om du inte gör klar anmälan direkt så kan du återkomma till den, ändra och lägga till. Du hittar den på:
%p= link_to team_url(@team), team_url(@team)
%p
= simple_format @team.race.regatta.confirmation
