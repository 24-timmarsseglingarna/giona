%h1 Listing people

%table
  %thead
    %tr
      %th Email
      %th First name
      %th Last name
      %th Co
      %th Street
      %th Zip
      %th City
      %th Birthday
      %th Phone
      %th External system
      %th External
      %th
      %th
      %th

  %tbody
    - @people.each do |person|
      %tr
        %td= person.email
        %td= person.first_name
        %td= person.last_name
        %td= person.co
        %td= person.street
        %td= person.zip
        %td= person.city
        %td= person.birthday
        %td= person.phone
        %td= person.external_system
        %td= person.external_id
        %td= link_to 'Show', person
        %td= link_to 'Edit', edit_person_path(person)
        %td= link_to 'Destroy', person, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Person', new_person_path
