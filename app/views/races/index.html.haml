%h1 Listing races

%table
  %thead
    %tr
      %th Start from
      %th Start to
      %th Period
      %th Common finish
      %th Mandatory common finish
      %th External system
      %th External
      %th Regatta
      %th
      %th
      %th

  %tbody
    - @races.each do |race|
      %tr
        %td= race.start_from
        %td= race.start_to
        %td= race.period
        %td= race.common_finish
        %td= race.mandatory_common_finish
        %td= race.external_system
        %td= race.external_id
        %td= race.regatta_id
        %td= link_to 'Show', race
        %td= link_to 'Edit', edit_race_path(race)
        %td= link_to 'Destroy', race, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Race', new_race_path
